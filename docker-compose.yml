services:
  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"   # 6380 avoids conflict with other projects on 6379
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  redis_data:
